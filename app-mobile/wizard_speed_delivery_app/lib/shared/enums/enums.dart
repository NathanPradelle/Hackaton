enum UserRole { chauffeur, client, admin }

enum StatutChauffeur { disponible, enCours, pause, indisponible }

enum StatutCommande { enAttente, confirmee, enCours, livree, annulee }

enum StatutTournee { planifiee, enCours, terminee, annulee }

enum StatutCamion { disponible, enCours, maintenance, horsService }

enum TypeEssence { diesel, essence, electrique }

enum PlageHoraire { matin, apresMidi, journee }
